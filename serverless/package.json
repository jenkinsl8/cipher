{
  "name": "serverless",
  "version": "1.0.0",
  "description": "This worker hosts the AI resume parser so the app can call it without putting an API key on the client device.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jszip": "^3.10.1"
  }
}
